import LinkItemsWrapper from "./LinkItemsWrapper";

const Navegation = ()=>{
    return(
        <nav className="hidden gap-4 p-3 md:flex">
            <LinkItemsWrapper/>
    </nav>
    );
};

export default Navegation;